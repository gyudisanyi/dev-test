syntax = "proto3";
package management;

service ManagementStudentService {
   rpc Update (Management) returns (Empty) {}
}

service ManagementProjectService {
  rpc Update (Management) returns (Empty) {}
}

message Empty {}
message Management {
   string id = 1;
   string student_ids = 2;
   string project_ids = 3;
   string createdAt = 4;
   string updatedAt = 5;
}

message ManagementRequestId {
   string id = 1;
}